spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yongdb
    username: yong
    password: mlp522639
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
